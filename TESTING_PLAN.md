# 📝 Go Chat Web - 集成测试计划 v1.0

**目标**: 验证前后端集成，确保所有核心功能按预期工作。

---

## 1. 测试环境

- **前端**: `http://localhost:3000`
- **后端**: `http://localhost:8080`
- **数据库**: PostgreSQL (本地 Docker)
- **缓存**: Redis (本地 Docker)
- **浏览器**: Chrome, Firefox, Safari (最新版)
- **测试账户**:
  - `user1@test.com` / `password123`
  - `user2@test.com` / `password123`

---

## 2. 测试用例

### 2.1 认证模块

| 用例 ID | 描述 | 预期结果 | 状态 |
|---|---|---|---|
| **AUTH-01** | **用户注册** - 使用新邮箱和密码注册 | 注册成功，自动登录并跳转到聊天页面 | `通过` |
| **AUTH-02** | **用户注册 (邮箱已存在)** - 使用已存在的邮箱注册 | 注册失败，提示“邮箱已存在” | `通过` |
| **AUTH-03** | **用户登录** - 使用正确的邮箱和密码登录 | 登录成功，获取 Token，跳转到聊天页面 | `通过` |
| **AUTH-04** | **用户登录 (密码错误)** - 使用错误的密码登录 | 登录失败，提示“邮箱或密码错误” | `通过` |
| **AUTH-05** | **用户登出** - 点击登出按钮 | 登出成功，清除 Token，跳转到登录页面 | `通过` |
| **AUTH-06** | **路由保护** - 未登录时访问 `/chat` | 重定向到 `/login` | `通过` |
| **AUTH-07** | **Token 刷新** - Access Token 过期后自动刷新 | 自动刷新 Token，API 请求无感知 | `通过` |

---

### 2.2 聊天模块

| 用例 ID | 描述 | 预期结果 | 状态 |
|---|---|---|---|
| **CHAT-01** | **获取聊天列表** - 登录后自动加载聊天列表 | 聊天列表正确显示，包含私聊和群聊 | `通过` |
| **CHAT-02** | **创建私聊** - 搜索并选择一个用户创建私聊 | 创建成功，聊天列表新增一个私聊项 | `通过`|
| **CHAT-03** | **切换聊天** - 在聊天列表中点击不同的聊天 | 右侧聊天窗口内容正确切换 | `通过` |
| **CHAT-04** | **创建群聊** - 点击“New Group”，输入群信息并添加成员 | 创建成功，群聊列表新增一个群聊项 | `通过` |
| **CHAT-05** | **群聊管理** - 添加/移除成员，修改群信息 | 操作成功，群成员和信息实时更新 | `失败` |
| **CHAT-06** | **退出/解散群聊** - 成员退出或群主解散群聊 | 操作成功，聊天列表移除该群聊 | `通过` |

---

### 2.3 消息模块

| 用例 ID | 描述 | 预期结果 | 状态 |
|---|---|---|---|
| **MSG-01** | **发送文本消息** - 在输入框输入文本并发送 | 消息立即显示（状态为 sending），然后变为 sent | `通过` |
| **MSG-02** | **接收文本消息** - 另一用户发送消息 | 收到新消息通知，消息列表自动更新 | `通过` |
| **MSG-03** | **消息状态** - 对方已读消息 | 消息状态变为已读（蓝色双勾） | `通过` |
| **MSG-04** | **编辑消息** - 编辑自己发送的消息 | 消息内容更新，并显示“已编辑”标记 | `通过` |
| **MSG-05** | **删除消息** - 删除自己发送的消息 | 消息从双方的聊天记录中移除 | `通过` |
| **MSG-06** | **回复消息** - 回复一条消息 | 发送的消息包含对原消息的引用 | `通过` |
| **MSG-07** | **发送图片** - 点击图片图标上传并发送图片 | 消息中显示图片预览 | `通过` |
| **MSG-08** | **发送文件** - 上传并发送文件（如 PDF） | 消息中显示文件链接和图标 | `通过` |
| **MSG-09** | **消息搜索** - 在聊天中搜索关键词 | 正确显示包含关键词的消息列表 | `失败` |
| **MSG-10** | **未读消息计数** - 收到未读消息 | 聊天列表对应项显示未读数，切换后清零 | `通过` |

---

### 2.4 WebSocket 实时通信

| 用例 ID | 描述 | 预期结果 | 状态 |
|---|---|---|---|
| **WS-01** | **用户在线状态** - 用户登录/登出 | 其他用户看到其在线状态实时变化 | `通过` |
| **WS-02** | **输入状态指示** - 用户在输入框输入时 | 对方聊天窗口显示“正在输入...” | `通过` |
| **WS-03** | **消息实时编辑** - 用户编辑消息 | 对方聊天窗口的消息内容实时更新 | `通过` |
| **WS-04** | **消息实时删除** - 用户删除消息 | 对方聊天窗口的消息实时消失 | `通过` |
| **WS-05** | **断线重连** - WebSocket 断开后自动重连 | 连接自动恢复，消息收发正常 | `通过` |

---

## 3. 测试执行

### 准备工作

1. **启动后端服务**:
   ```bash
   cd go-chat-backend
   docker-compose up -d
   go run cmd/main.go
   ```

2. **启动前端服务**:
   ```bash
   cd go-chat-web
   npm install
   npm run dev
   ```

3. **打开两个浏览器窗口**:
   - 窗口 A: 登录 `user1@test.com`
   - 窗口 B: 登录 `user2@test.com`

### 执行步骤

- 按照上述测试用例逐一执行。
- 记录实际结果，与预期结果进行对比。
- 对于失败的用例，记录详细的复现步骤、截图和日志。

---

## 4. 缺陷管理

- **工具**: GitHub Issues
- **标签**: `bug`, `frontend`, `backend`, `critical`, `high`, `medium`, `low`
- **流程**:
  1. 发现 Bug -> 创建 Issue
  2. 分配负责人 -> 修复 Bug
  3. 提交 PR -> Code Review
  4. 合并代码 -> 回归测试
  5. 关闭 Issue

---
